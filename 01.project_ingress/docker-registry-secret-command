kubectl create secret docker-registry cloudenthusiastic-registry-secret \
  --docker-server=https://index.docker.io/v1/ \
  --docker-username=cloudenthusiastic \
  --docker-password=<docker-password> \
  --docker-email=<email-id> \
  --dry-run=client -o yaml > docker-registry-secret.yaml


Then, add the secret to your deployment.yml under spec.template.spec:
yaml
Copy name of secrets

at port level  
imagePullPolicy: Always

spec:
  imagePullSecrets:
    - name: cloudenthusiastic-registry-secret

